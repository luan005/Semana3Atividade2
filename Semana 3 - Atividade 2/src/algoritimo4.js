const arr = [1,2,3,4,5]; // Declara um array de numeros
const output = arr.filter((x) => x > 4);//Usa o filter com um arrow function para retornar numeros maiores que 4
console.log(output); // Exibe os numeros maiores do que 4

